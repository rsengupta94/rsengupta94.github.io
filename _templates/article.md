---
# Copy this file to _posts/YYYY-MM-DD-slug.md and fill it in.
#
# The DATE in the filename must be today or earlier — Jekyll silently skips
# future-dated posts, and the only symptom is a page that isn't there.
# The SLUG in the filename becomes the URL (/articles/slug/) and can never
# change once published. `title:` below can change freely; the filename cannot.

title: ""

# The search-result snippet and the summary shown on the articles index.
# Target 120–145 characters — articles render a publish date, which shortens
# what Google displays. Drafted via the seo-summary skill, from your own text.
description: >-

# Reuse an existing tag where one fits rather than inventing a new category:
#   grep -h '^tag:' _posts/*.md | sort -u
tag: ""

# Optional. Only when this article replaces a URL that was already published —
# see the retire-content skill before using it.
# redirect_from:
#   - /articles/old-slug/
---

Body goes here, exactly as written. No `read_time` field — read time is
computed from the word count at build time.
