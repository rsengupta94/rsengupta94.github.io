---
# Copy this file to _work/slug.md and fill it in. No date in the filename.
# The slug becomes the URL (/work/slug/) and can never change once published.
#
# Every value below is a fact about work you did. Leave a TODO rather than a
# plausible guess — nothing here should be invented.

title: ""

# The search-result snippet. Target up to 155 characters: case-study pages
# render no date, so Google shows more of it than it does for an article.
description: >-

org: ""                 # employer or client
period: ""              # e.g. "2024 — Now"
role: ""                # what you personally did
outcome: ""             # headline result, shown in the left column of /work/

# One or two sentences for the index row, in your words.
summary: >-

tags: []                # e.g. [Python, Kafka, AWS]

# Position on /work/ — lower sorts first. NOT optional: a case study without
# `order` sorts above everything else. It is not a URL, so renumbering is free.
order:

# The left-to-right diagram on the case-study page, and the arrow chain on the
# index. Four to six stages reads best; past six it wraps awkwardly.
# `label` is the component, `sub` is the one-line detail under it.
# Quote any value containing a colon or a "·".
schema:
  - { label: "", sub: "" }
  - { label: "", sub: "" }
  - { label: "", sub: "" }
  - { label: "", sub: "" }
---

The write-up goes here, exactly as written.
